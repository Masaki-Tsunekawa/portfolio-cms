export enum Status {
  Disabled = 0,
  Enabled,
}

export enum Division {
  General = 0,
  Administrator,
}

export enum ItemContition {
  Normal = 1,
  Broken,
  Lending,
  BeingRepaired,
  ToBeDisposed,
  BeenDisposed,
}
